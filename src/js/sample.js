var tdd = require('./TDDforJS');
var testCase = tdd.testCase;
var test = tdd.test;

testCase("wowowowowow", function(){
   test("something crazy", function(){

   }/*,true to see the full msg */);
}/*,false to disable all logs */);
